/**
 * One definition of the SukkaTube mark, shared by the UI components and by the
 * generated images (browser tab, home screen, link previews), so they can never
 * drift apart.
 */

export const BRAND_RED = '#dc2626'

/** The play triangle, drawn in a 24×24 viewBox. */
export const PLAY_PATH = 'M8 5.5v13l11-6.5-11-6.5Z'
